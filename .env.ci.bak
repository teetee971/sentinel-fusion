# --- GitHub ---
GH_USER=TonUserGithub                           # ton user GitHub
REPO_NAME=sentinel-fusion                       # nom du dépôt à créer/pousser

# --- Cloudflare (pour le workflow GitHub) ---
CF_API_TOKEN=cf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx    # Token Pages:Edit
CLOUDFLARE_ACCOUNT_ID=xxxxxxxxxxxxxxxxxxxxxxxx
CLOUDFLARE_PROJECT_NAME=sentinel-fusion
CLOUDFLARE_PAGES_BRANCH=main
GH_USER=tonUserGithub
REPO_NAME=sentinel-fusion
GH_TOKEN=# 2) Nettoie tout espace/retour chariot éventuelGH_TOKEN="$(printf %s "$GH_TOKEN" | tr -d ' rn')"export GH_TOKEN# 3) Vérifie qu’il est bien présent et “propre”echo -n "$GH_TOKEN" | wc -c      # doit afficher ~40+ caractères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
